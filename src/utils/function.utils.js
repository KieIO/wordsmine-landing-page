// export const getCookie = (key) => {
//     return document.cookie
//         .split('; ')
//         .find(row => row.startsWith(key))
//         .split('=')[1];
// }

// export const setCookie = (key, value, expireTime) => {
//     return document.cookie = `${key}=${value};expires=${expireTime};path=/`
// }